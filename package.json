{
  "name": "market-news-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "check": "node --check api/*.js && node --check lib/*.js && node --check lib/nsl/*.js && node --check lib/patterns/*.js && node --check lib/research/*.js",
    "gen:source": "node scripts/gen-full-source.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@vercel/blob": "^2.4.0"
  }
}
