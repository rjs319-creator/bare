# Experiment #9 — OMEGA-Swing, Survivorship-Free (2026-07)

**Question.** The app's `op=omegawf` replays OMEGA over a STATIC present-day universe, so it stamps
`survivorshipSafe=false` — which structurally blocks any challenger promotion. This re-runs the
**identical OMEGA scorer, executable fill model, cost model, and fail-closed gates** over a
survivorship-COMPLETE cross-section (delisted names included up to their last trading day, via the
`pit-secmaster-v1` master), and asks whether OMEGA's ranking survives once the dead names are in.

## Method
- **Universe:** every name in-band ($300M–$10B cap, ≥$3M/day ADV) **as of each decision date**, from
  `research/lib/secmaster`; delisted names included up to their last trading day. Candidate set = union
  of the in-band cross-sections at 2022-06-30, 2023-06-30, 2024-06-30 → 2213 names, 1006 with enough history (600 active today, 406 since delisted).
- **Scorer:** the app's own `lib/omega-swing.evaluateCandidate` — identical 0–100 score, tier, features.
- **Execution:** `lib/omega-execution.planOmegaEntry` — the T+1 next-open / breakout-stop / pullback-limit
  fill; a gap-through, no-trigger, or delist-next-day is an honest **no-trade** (14107 dropped).
- **Label (leakage-safe):** survivorship-free forward residual FROM THE FILL — stock raw return minus a
  0.6·SPY + 0.4·sector-ETF move (sector where cached: XLI, XLY, else market-only),
  net of the tiered round-trip cost. A name that **delists inside the window** takes the Shumway (1997)
  −30% terminal haircut and is NOT dropped (that would re-introduce the bias). OMEGA's own
  `residualForward` is deliberately NOT used here — it returns pending on a truncated window.
- **Dates:** 25 month-ends (2022-06-30 → 2024-06-30), the delisting-dense window.

## Result

| Metric | Survivorship-free | Survivor-only |
|---|---|---|
| Score → 10d residual rank-IC | -0.0269 (t -1.30, 25 dates) | -0.0125 (t -0.44, 24 dates) |
| 10d momentum baseline rank-IC | 0.0289 (t 0.87, 25 dates) | — |
| relVol baseline rank-IC | 0.0109 (t 0.55, 25 dates) | — |
| Mean raw 10d return | 1.28% | 1.63% |

- **Survivorship return bias (survivor − free):** **0.36%** over 10d.
- **Panel:** 5053 scored name-dates with a resolved residual, of which **27** were delistings taking the Shumway haircut.
- **Tier payoff (cost-net 10d residual):** PRIME n/a (n=0) · QUALIFIED n/a (n=0) · WATCH -0.59% (n=237).
- **Purged block ICs:** -0.0486, 0.0039, 0.0007, -0.0914.

## Verdict — `no-edge`

Fail-closed gates (`lib/omega-backfill.evaluateGates`, the SAME ones the app uses):

| Gate | Pass |
|---|---|
| ≥3 purged blocks all positive | false |
| mean OOS IC > margin | false |
| not deadline-truncated | true |
| tier payoff monotone | false |
| beats every simple baseline | false |
| **survivorship-safe** | **true** |
| live-funnel parity | false |

- **passed (statistical edge): false** · **promotable: false**

## Reading
- **The survivorship flag is discharged for OMEGA's evidence.** This harness draws its universe from
  the survivorship-complete master, so `survivorshipSafe=true` — the one gate the app's static-universe
  replay can never satisfy. OMEGA's ranking does NOT clear the statistical edge gates survivorship-free — consistent with the app's standing "no durable regime-robust selection edge on EOD/free data" verdict. A non-edge result here is expected and honest, not a bug.
- **promotable is still false** because **live-funnel parity is false** — this
  research replay is not the live `op=today` funnel (Phase 4 capture accrues that separately, going
  forward). Both must hold before a challenger could ever be promoted; the gates enforce it.
- Survivorship biases the return **level** by 0.36%/10d (survivor-only flatters), echoing #8's finding that survivorship shifts the level, not necessarily the ordering.

## Limits
- Delisting date ≈ last traded bar; Shumway −30% is a blanket penalty (mergers + bankruptcies pooled).
- Sector-relative only where the SPDR ETF is cached (XLI, XLY); other sectors are market-relative.
- Neutral regime (no research-side macro feed) — OMEGA's risk-off penalty lever is inactive here.
- Coverage is the ~10k-symbol FMP cache (2021–2026); one delisting-dense window; 1006 names.

_Generated by research/53-omega-survivorship-free.js in 25.3s. Shadow research — never affects live ranks. survivorshipSafe=true, historicalLiveParity=false._
